local _M = {}
local _P = {}

local ECmdId = {
		
}

function _M.ModuleInit(isReload)
	local send = {
		
	}
	local recv = {
		
	}
	_M.Send = SocketMgr.Regist(, send, recv, isReload)
end

function _M.ModuleClean()

end

return _M